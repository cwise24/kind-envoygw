---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: btp-lb
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: ngx-route
  loadBalancer:
    type: RoudRobin #Random, LeastRequest, ConsistentHash
    slowStart: true